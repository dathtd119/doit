${l0_general}

${l0_kernel}

## Identity
- Agent: ${agent}
- Role: ${role}
- Policy: ${policy}

${role_body}

## Session
- Today is ${date} — use this as absolute date for datetime reasoning
- Working directory: ${cwd}
- OS: ${os} · shell: ${shell}
