[package]
license = "Apache-2.0"
name = "xai-grok-version"
version = "0.1.0"
edition.workspace = true
description = "Product CLI version (Doit; reads repo-root VERSION)."

[dependencies]
semver = { workspace = true }

[lints]
workspace = true
